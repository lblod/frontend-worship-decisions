<div {{did-update this.updateSelectedValue @value}}>
  <PowerSelectMultiple
          @search={{perform this.search}}
          @searchField="naam"
          @searchPlaceholder="Typ om te zoeken..."
          @options={{this.options}}
          @searchEnabled={{true}}
          @loadingMessage="Aan het laden..."
          @selected={{this.selected}}
          @onChange={{this.changeSelected}}
          @allowClear={{true}} as |bestuurseenheid|>
    {{bestuurseenheid.naam}} ({{bestuurseenheid.classificatie.label}})
  </PowerSelectMultiple>
  <AuHelpText class="au-u-margin-none">vb. Gemeente Hasselt, OCMW Gent</AuHelpText>
</div>
