pipeline:
  build:
    image: plugins/docker
    settings:
      repo: ${CI_REPO}
      tags: latest
    secrets: [ docker_username, docker_password]
when:
  branch: ${CI_REPO_DEFAULT_BRANCH}
  event: push
